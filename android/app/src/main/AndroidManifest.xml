<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- File-based storage uses internal app dir (getApplicationSupportDirectory).
         No external-storage permissions are needed on Android 10+ (API 29+).
         READ/WRITE_EXTERNAL_STORAGE are scoped to API ≤28 only for legacy devices.
         MANAGE_EXTERNAL_STORAGE has been removed: the app does not manage arbitrary
         external files and Google Play policy prohibits this permission unless the
         app is a file manager, antivirus, or backup tool. -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- 🌐 Internet & network state -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- Images and pdf permissions for Android 13+-->
    <!-- Images ke liye -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- PDFs / Files ke liye -->
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission
        android:name="android.permission.USE_FULL_SCREEN_INTENT"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.ACCESS_NOTIFICATION_POLICY"
        tools:node="remove" />

    <!-- 📲 Notifications (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 🏃 Step counting (TYPE_STEP_DETECTOR via ACTIVITY_RECOGNITION only) -->
    <!-- BODY_SENSORS / BODY_SENSORS_BACKGROUND intentionally removed:
         Sleep is tracked via screen-state (SCREEN_ON/OFF), not body sensors.
         Step tracking uses TYPE_STEP_DETECTOR, covered by ACTIVITY_RECOGNITION alone. -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <!-- ⚙️ Foreground service types -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH" />
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"
        tools:node="remove" />

    <!-- 🔋 Battery optimization ignore -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- 📷 Camera -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission
        android:name="android.permission.RECORD_AUDIO"
        tools:node="remove" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <application
        android:label="Snevva"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:showWhenLocked="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:enableOnBackInvokedCallback="true"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="com.google.android.gms.credentials.API_ENABLED"
                android:value="true" />

            <meta-data
                android:name="com.google.android.gms.credentials.CredentialPickerConfig"
                android:value="true" />

            <meta-data
                android:name="androidx.credentials.CREDENTIAL_SERVICE_ENABLED"
                android:value="true" />

            <!-- Theme shown before Flutter UI renders -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <meta-data
                android:name="flutterEmbedding"
                android:value="2" />

            <!-- 🔥 Small icon for foreground service notification (CRITICAL for Android 12+) -->
            <meta-data
                android:name="flutterBackgroundServiceNotificationIcon"
                android:resource="@drawable/ic_stat_notification_bg" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 🔔 Local notifications receivers -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />

        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Boot Receiver for rescheduling alarms and workers -->
        <receiver android:name=".BootReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Sparse wakeup receiver for step counter -->
        <receiver android:name=".SparseWakeupReceiver" android:exported="false" />

        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.FlutterLocalNotificationsReceiver" />

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="high_importance_channel" />

        <!-- 🦶 Step Tracking Background Service (Native) -->
        <service
            android:name=".StepCounterService"
            android:exported="false"
            android:foregroundServiceType="dataSync|health"
            tools:replace="android:exported,android:foregroundServiceType" />

        <!-- 🛌 Flutter Background Service (Sleep Tracking + Step Event Relay) -->
        <meta-data
            android:name="id.flutter.flutter_background_service.notification_channel_id"
            android:value="tracker_channel"/>
        <meta-data
            android:name="id.flutter.flutter_background_service.notification_channel_name"
            android:value="Health Tracking"/>
        <meta-data
            android:name="id.flutter.flutter_background_service.notification_channel_desc"
            android:value="Step &amp; sleep tracking"/>

        <!-- ⚙️ Override flutter_background_service's BackgroundService to add foregroundServiceType
             Required on Android 14+ (targetSdk 34+) — MissingForegroundServiceTypeException -->
        <service
            android:name="id.flutter.flutter_background_service.BackgroundService"
            android:foregroundServiceType="dataSync"
            tools:replace="android:foregroundServiceType" />

        <!-- NotificationOnKillService removed: WorkManager-based rescheduling
             ensures reminders are refreshed every 6h even after app kill.
             The misleading "Your alarms may not ring" notification is suppressed
             both here (service removed) and via warningNotificationOnKill: false
             on all AlarmSettings in Dart. -->

        <service
            android:name=".SleepNoticingService"
            android:exported="false"
            android:foregroundServiceType="dataSync|health" />

        <receiver android:name=".StartSleepReceiver" android:exported="false" />
        <receiver android:name=".StopSleepReceiver" android:exported="false" />

        <!-- 📡 Network connectivity receiver (triggers file-based sync queue on reconnect) -->
        <receiver
            android:name=".ConnectivityReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>

        <!-- 🔔 Native Reminder Alarm Receiver (survives OEM kill + reboot) -->
        <receiver
            android:name=".ReminderAlarmReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.coretegra.snevvaa.REMINDER_FIRE" />
            </intent-filter>
        </receiver>

        <!-- Stop action receiver for reminder notification -->
        <receiver
            android:name=".ReminderStopReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.coretegra.snevvaa.REMINDER_STOP" />
            </intent-filter>
        </receiver>

        <!-- Worker Initialization (if needed, usually auto-inits) -->
    </application>

    <!-- 🧭 Queries (text processing support) -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
    </queries>
</manifest>
