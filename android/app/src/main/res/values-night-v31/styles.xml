<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">

        <!-- Splash background -->
        <item name="android:windowSplashScreenBackground">
            @android:color/black
        </item>

        <!-- Splash icon (VECTOR) -->
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/snevva_elly_foreground</item>


    </style>

</resources>
